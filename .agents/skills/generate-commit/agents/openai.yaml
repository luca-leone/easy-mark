interface:
  display_name: "Generate Commit"
  short_description: "Propose and validate semantic Conventional Commits"
  default_prompt: "Use $generate-commit to inspect the staged diff and propose a validated Conventional Commit without staging or committing unless I explicitly ask."
policy:
  allow_implicit_invocation: false
