interface:
  display_name: "Auto Commit"
  short_description: "Stage verified task changes, commit deterministically, and report version/tag proposals"
  default_prompt: "Use $auto-commit after verification passes to run npm run task:commit and report the commit plus version/tag proposal without pushing."
policy:
  allow_implicit_invocation: false
